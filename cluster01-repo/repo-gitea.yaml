apiVersion: infra.nephio.org/v1alpha1
kind: Repository
metadata: # kpt-merge: default/example-site-name
  name: example-site-name
  namespace: default
  annotations:
    internal.kpt.dev/upstream-identifier: 'infra.nephio.org|Repository|default|example-site-name'
spec:
  description: example-site-name repository
  defaultBranch: main
